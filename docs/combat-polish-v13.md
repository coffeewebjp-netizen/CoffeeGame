# Combat Polish V13 — 2026-09-06

ORC-20260906-006 / IN03. V12のプレイ指摘に対応。現在の赤羽織の剣の少女、猫少女、竜の少女の役割とモデルは維持する。

## 操作と変更

- 剣の少女の現在の居合必殺が溜めを終えて発動する時に「しんじゃえ」を1回再生する。ST不足・溜めの中断では再生しない。先に提案した三段階の新必殺技は今回の依頼外で、この音声の採用条件にはしない。
- デバッグ版の「システム」最下部にライバル親密度を追加。猫少女と竜の少女それぞれに−10%、＋10%、0%、100%のボタンがある。マウスとコントローラーの設定行ナビゲーションに対応。非Developmentビルドには含めない。
- 親密度は0〜100に制限し、100で既存の加入記録・猫のパーティ生成につなぐ。猫加入後のライバルは竜の少女へ進む。加入済みの仲間は数値を下げても解除しない。レベル・回復状態・獲得済み報酬を維持し、経験値や学習報酬は追加しない。実プレイでは既存のプロフィール保存経路を使い、セーブ形式は変更しない。
- R3/Rのロックを保持したまま、0.65秒の移動継続で走行に入ると移動方向へ向く。歩き・停止・防御は敵を向き、走行から攻撃しても敵へ向き直る。側転・バク転・回避・落下突きの途中姿勢は従来通り保持する。
- 回避無敵を約0.41秒から約0.61秒に延長。着地時にも終了し、常時無敵にはならない。ジャスト回避は開始後0.30秒以内。
- 敵の実際の命中時刻に、回避開始位置がその攻撃範囲・高さ・向きに含まれるか確認する。範囲外へ脱出したために`ApplyDamage`まで届かない回避も成功として扱う。現在位置で無敵回避した命中も従来通り対象。同じ回避・同じ攻撃ではST満タンと演出は1回だけ。予備動作だけ、キャンセル済み攻撃、遠方の空振り、早すぎる回避、通常ジャンプには付与しない。
- 成功時の演出は0.65秒。世界がグレーになり速度0.18倍から自然に戻る。ポーズと猫の時間停止との共存はV12を維持。

## 音声の出典

既存Owner素材`C:/work/media-assets/dragon_girl_finisher_20260906_v1/dragon_finisher_shinchyae.wav`を変更せず、`Resources/Audio/Voices/Heroine/special_01.wav`へコピー。SHA-256: `8fee27af8d6b6759bfc82c85e57173a81ae93d211d3dddedd6982d1114cf105f`。ファイル名にdragonとあっても、Owner指定の割り当て先は剣の少女。再生成・音程加工なし。

## 検証・復元

Unity 6000.5.7f1。モデル・コントローラーのsetupを再実行しない専用ビルド。Domain/Input/Runtimeの回帰テスト314件が成功。初回は旧無敵時間の期待値と親密度上限の2件を検出し、修正後に全件再実行した。敵コントローラーの実攻撃からの回避・範囲外脱出・取消・遅延・通常ジャンプを検証している。

確認用ビルド: `unity/CoffeeGame/Builds/Windows-CombatPolishV13/CoffeeGAME-CombatPolishV13.exe`。通常起動先は`unity/CoffeeGame/Builds/Windows/CoffeeGAME.exe`。証跡は`C:/work/task-backups/ORC-20260906-006/revision-03`に保存する。`-captureCombatPolish`は空の一時プロフィールを使い、実セーブ・クラウドへの書き込みを止めて動作確認する。

今回の通常版反映前バックアップは`.task-local-backup/ORC-20260906-006-revision-03-normal-player`。ゲーム終了後に`tools/restore-pre-combat-polish-v13-player.cmd`で直前のV12へ復元する。`.ps1`を引数なしで実行すると照合のみ。進行と現在のキャラクター選択は維持する。公開ソースpushは従来通り保留。

最終結果：`runtime-02`の24項目が成功し、設定・走行・必殺発動・実攻撃からの回避グレー・色の復帰の5画像を確認した。初回診断は加入によるUI再生成で破棄待ちの旧ボタンまで参照して停止したため、現行の設定行リストを参照するよう診断コードを修正。両診断で実セーブは不変、入力・表示設定を復元済み。スライムが前進しても範囲外まで脱出したことを明示する追加確認は関連7件が成功。

22:23:40 JSTに通常版へ反映し、新297ファイルと旧348ファイルをハッシュ照合。通常版DLLは実行確認済みビルドと同一。復元の照合のみ実行も成功。作業前1,960ファイルの範囲外差分は0件。Unityの自動変更2設定は当初未変更であることを確認して元のハッシュへ復元した。この改修後にSteam UIから起動したとは主張せず、通常起動先への同一ペイロード反映を確認範囲とする。
